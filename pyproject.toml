[project]

name = "ai-meal-analyzer"

version = "0.1.0"

requires-python = ">=3.10"

dependencies = [
    "openai",
    "python-dotenv",
    "pydantic",
]

[project.optional-dependencies]
dev = [
    "pytest",
]